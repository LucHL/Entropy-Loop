using UnityEngine;

public class Cavalier : Units
{
    protected override void Start()
    {
        // speed = 10f;
        // totalHealth = 80;
        // damagePerAttack = 25;
        // entityTag = "Enemy";
        base.Start();
    }
}